name: Apply Customer List stable actions once

on:
  push:
    branches:
      - fix/customer-list-stable-sale-actions-20260902
    paths:
      - .github/workflows/apply-customer-list-stable-actions-once.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repair branch
        uses: actions/checkout@v5
        with:
          ref: fix/customer-list-stable-sale-actions-20260902
          fetch-depth: 0

      - name: Set up Node 22
        uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Apply stable identities and final-record filtering
        run: python3 .github/scripts/apply-customer-list-stable-actions.py

      - name: Check affected JavaScript syntax
        shell: bash
        run: |
          set -euo pipefail
          node --check app-accounting-records.js
          node --check app-customer-list-approval-refresh.js
          node --check customer-list-stable-actions.test.mjs

      - name: Verify frozen Customer List actions
        run: node --test customer-list-stable-actions.test.mjs

      - name: Verify existing Customer List edit and return contracts
        run: node --test customer-list-admin-edit.test.mjs

      - name: Verify update recovery contract
        run: node --test app-update-recovery.test.mjs

      - name: Verify organization access cache contract
        run: node --test organization-access-gate.test.mjs

      - name: Verify sale completion runtime contract
        run: node --test sale-completion-runtime.test.mjs

      - name: Commit verified repair
        shell: bash
        run: |
          set -euo pipefail
          rm .github/scripts/apply-customer-list-stable-actions.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            app-accounting-records.js \
            app-customer-list-approval-refresh.js \
            supabase/functions/accounting-records/index.ts \
            index.html \
            service-worker.js \
            organization-access-gate.test.mjs \
            sale-completion-runtime.test.mjs \
            .github/workflows/field-coach-android-beta5-sale-runtime.yml \
            customer-list-stable-actions.test.mjs \
            .github/scripts/apply-customer-list-stable-actions.py
          git diff --cached --check
          git commit -m "Restore stable Customer List sale actions"
          git push origin HEAD:fix/customer-list-stable-sale-actions-20260902
