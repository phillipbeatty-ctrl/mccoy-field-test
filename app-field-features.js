// Release switch: nearest-lead selection and its automatic arrival/departure
// workflow are paused. Keep their implementations for a future reviewed release.
// Missing configuration also means paused; browser storage cannot enable it.
window.MCCOY_FIELD_FEATURES=Object.freeze({automaticNearestLead:false});
