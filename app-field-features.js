// Release switch: nearest-lead selection and its automatic arrival/departure
// workflow are enabled as of 2026-09-17. Missing configuration still means
// paused; browser storage cannot enable it.
window.MCCOY_FIELD_FEATURES=Object.freeze({automaticNearestLead:true});
