# SPOTIO Final Pre-Merge State

The implementation branch contains the canonical import schema, canonical upsert logic, updated `spotio-import` and `lead-admin` functions, regression coverage, and the release acceptance plan. Production functions are active. The pull request should remain draft until the sequential-upload acceptance test passes.
