# SPOTIO Open Release Gate

The implementation is deployed and source-controlled, but the release gate remains open until the sequential-upload acceptance test is completed on redacted test leads. This prevents production data from being used as an uncontrolled test fixture.
