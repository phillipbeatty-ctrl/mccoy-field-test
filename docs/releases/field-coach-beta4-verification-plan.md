# Verification plan

The final branch head must pass source-integrity, browser/PWA, mobile release, Android package, web archive, update recovery, access gate, Auth email, and SPOTIO regression workflows. Vercel must deploy the same head successfully before merge.
